<div class="form">
  <%= link_to "🔙", cocktails_path, class: "back-btn" %>
  <div class="form-box">
    <div class="subtitle">Create a Cocktail</div>
      <%= simple_form_for @cocktail do |f| %>
        <div class="field">
          <%= f.input :name,  class: "rounded",placeholder: "Mojito", label: false %>
        </div>
        <div class="field">
          <%= f.input :photo, as: :file %>
        </div>
        <div class="field">
         <%= f.submit "Create", class: "dose-btn" %>
        </div>
      <% end %>
    </div>
</div>

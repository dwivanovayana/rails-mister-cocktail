<div class="banner">
  <h1>Create your own cocktail recipe.</h1>
  <%= link_to "Add Cocktail", new_cocktail_path, class:"button" %>
</div>

<div class="container mt-3 d-flex flex-row">
  <div class="cards">
  <div class="card">
    <% @cocktails.each do |cocktail| %>
    <%= cl_image_tag cocktail.photo, width:400, height:500, crop: :fill %>
    <div class="card-category">
      <%= link_to cocktail.name, cocktail_path(cocktail) %>
      <%= link_to " 🗑", cocktail_path(cocktail), method: :delete, data: { confirm: "Are you sure?" } %>
    </div>
    <% end %>
  </div>
</div>
</div>
